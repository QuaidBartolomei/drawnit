{
  "engines": {
    "node": "^16"
  },
  "name": "drawnit",
  "version": "1.0.0",
  "description": "",
  "main": "server/src/main.js",
  "scripts": {
    "start": "npm run server",
    "dev": "concurrently  \"npm run server:dev\" \"npm run client\" ",
    "build": "npm run server:build && npm run client:build",
    "client": "cd client && npm run start",
    "client:build": "cd client && npm ci --production=false && npm run build && npm prune --production",
    "client:dev": "cd client && npm run dev",
    "client:install": "cd client && npm install",
    "client:lint-staged": "cd client && eslint --fix --quiet --cache",
    "client:test": "concurrently  \"npm run server:dev\" \"cd client && npm run test\" ",
    "full-install": "npm install && npm run server:install && npm run client:install",
    "test:all": "concurrently  \"npm run server:test\" \"npm run client:test\" ",
    "server": "cd server && npm start",
    "server:build": "cd server && npm ci --production=false && npm run build && npm prune --production",
    "server:dev": "cd server && npm run dev",
    "server:install": "cd server && npm install",
    "server:test": "cd server && npm run test",
    "format": "prettier ./ --ignore-unknown --write",
    "prepare": "husky install"
  },
  "author": "Quaid Bartolomei",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "prettier": "2.5.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2"
  }
}
